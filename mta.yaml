_schema-version: 3.3.0
ID: Employees_Management_Project
description: A simple CAP project.
version: 1.0.0
modules:
- name: Employees_Management_Project-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Employees_Management_Project-auth
  - name: Employees_Management_Project-db
  - name: Employees_Management_Project-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: Employees_Management_Project-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Employees_Management_Project-db
  parameters:
    buildpack: nodejs_buildpack
- name: Employees_Management_Project
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: Employees_Management_Project-auth
  - name: Employees_Management_Project-destination
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: Employees_Management_Project-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Employees_Management_Project-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - project1.zip
      name: project1
      target-path: resources/
- name: project1
  type: html5
  path: app/project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Employees_Management_Project-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: Employees_Management_Project-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: Employees_Management_Project-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Employees_Management_Project-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: Employees_Management_Project-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Employees_Management_Project-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
